package;

class Main
{
	public static var splitGrps:String;

	public static var nextMessage:Array<String> = ['src/missions/mission$mission'];

	public static function missions()
	{
		public static var missions:String;

		nextMessage(forEach);

		for (splitGrps in missions)
		{
			nextMessage.split();

			forEach(nextMessage);
			spliting(nextMessage);

			// var string:String;
			numPad.pad();

			trace(nextMessage);
		}

		for (splitInNumbers in nextMessage)
		{
			nextMessage.forEach(splitInNumbers);
			forEachMessage(output[1]);
			trace(nextMessage);
		}

		function splitWordSideways()
		{
			// splits into 2's
			pie(5);
			splits(2);

			split(pie);
			pairs.split('-- ---');

			nextMessage.pie();
			trace(pie, + nextMessage);

			secondPie(3);
			splitGrps(1);

			trace(splitWordSideways);
		}

		function mathProblems()
		{
			for (math in i...num + secondNum)
			{
				Sys.output(19);
				trace(mathProblems);
			}
		}
	}
}
